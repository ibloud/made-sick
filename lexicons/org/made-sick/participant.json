{
  "lexicon": 1,
  "id": "org.made-sick.participant",
  "defs": {
    "main": {
      "type": "record",
      "key": "literal:self",
      "record": {
        "type": "object",
        "required": ["version", "directory", "displayName", "consentedAt"],
        "properties": {
          "version": { "type": "integer", "minimum": 1, "maximum": 1 },
          "directory": { "type": "boolean" },
          "displayName": { "type": "string", "maxLength": 240 },
          "milestone": { "type": "string", "maxLength": 1500 },
          "consentedAt": { "type": "string", "format": "datetime" }
        }
      }
    }
  }
}
